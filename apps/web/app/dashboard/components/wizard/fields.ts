import styles from "./wizard.module.css";

/** Shared layout classes for step components. */
export const fieldStyles = {
  fields: styles.fields!,
  field: styles.field!,
  wide: styles.wide!,
  intro: styles.intro!,
  /** A list of radio/checkbox cards (e.g. pick a client, a style, a finish). */
  choices: styles.choices!,
  choice: styles.choice!,
  choiceText: styles.choiceText!,
  choiceHint: styles.choiceHint!,
};
