import styles from "./wizard.module.css";

/** Shared field layout classes for step components. */
export const fieldStyles = {
  fields: styles.fields!,
  field: styles.field!,
  wide: styles.wide!,
};
