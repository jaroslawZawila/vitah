// Features built but switched off in the app for now.

/**
 * Push notifications: the Perfil toggles and registering the phone. Off
 * until push is set up (an EAS project id, and a development/store build on
 * Android). The server side is ready and sends nothing while no phone is
 * registered.
 */
export const PUSH_NOTIFICATIONS = false;
